<!DOCTYPE html>
<html>
<head>
    <style>
        /* Style the navigation bar container */
        nav {
            background-color: #333;
            color: #fff;
            padding: 10px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        /* Style the logo link */
        .logo {
            font-size: 24px;
            text-decoration: none;
            color: #fff;
        }

        /* Style the search form */
        .search-container {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .search-form {
            display: flex;
            align-items: center;
        }

        /* Style the label for the search input */
        label {
            margin-right: 10px;
            font-size: 16px;
            color: #fff;
        }

        /* Style the search input */
        #episode {
            padding: 8px;
            border: 2px solid #fff;
            border-radius: 5px;
            background-color: transparent;
            color: #fff;
        }

        /* Style the search button */
        button[type="submit"] {
            padding: 8px 12px;
            background-color: #4CAF50;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        /* Style the menu */
        .menu-container {
            display: flex;
            align-items: center;
        }

        .menu {
            list-style: none;
            display: flex;
            margin: 0;
            padding: 0;
        }

        .menu li {
            margin-right: 20px;
        }

        /* Style the menu links */
        .menu a {
            text-decoration: none;
            color: #fff;
        }

        /* Add a hover effect to the menu links */
        .menu a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
<nav>
    <a class="logo" href="/"></a>
    <div class="search-container">
        <form class="search-form" action="/search" method="GET">
            <label for="episode">Search for episodes:</label>
            <input type="text" id="search" name="search" placeholder="Enter episode...">
            <button type="submit">Search</button>
        </form>
    </div>
    <div class="menu-container">
        <ul class="menu">
            <li><a href="/">Home</a></li>
            <li><a href="/episodes">All Episodes</a></li>
        </ul>
    </div>
</nav>
</body>
</html>
